import 'package:flutter/material.dart';
import 'components/connect_device_body.dart';

class ConnectDevice extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ConnectDeviceBody(),
    );
  }
}
